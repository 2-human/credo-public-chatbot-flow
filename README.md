# Credo chatbot — design preview

Working design preview of the Credo Legal Tidio chatbot flow, for review by the team.

**Live preview:** https://tnosugar.github.io/credo-public-chatbot-flow/

Walk through both in-hours and after-hours variants via the mode toggle in the sidebar. The same conversation renders side-by-side as a desktop corner widget and a mobile near-fullscreen takeover, so you can pressure-test the UX in both breakpoints.

This mirror is auto-synced from `public/chatbot-flow/` in the private working repo on every push to `main`. The Tidio implementation (the importable flow JSON, the build sheet, and the design spec) lives privately; this page mirrors the locked design only.
